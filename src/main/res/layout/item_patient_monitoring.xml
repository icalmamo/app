<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="4dp"
    android:layout_marginEnd="4dp"
    android:layout_marginTop="8dp"
    android:layout_marginBottom="8dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="2dp"
    app:strokeColor="#E0E0E0"
    app:strokeWidth="0.5dp"
    app:cardBackgroundColor="@android:color/white"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?android:attr/selectableItemBackground">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:minHeight="100dp">

        <!-- Patient Avatar/Icon -->
        <View
            android:id="@+id/viewPatientAvatar"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:background="@drawable/patient_avatar_background"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

        <TextView
            android:id="@+id/textPatientIcon"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:text="👤"
            android:textSize="28sp"
            android:gravity="center"
            app:layout_constraintTop_toTopOf="@id/viewPatientAvatar"
            app:layout_constraintBottom_toBottomOf="@id/viewPatientAvatar"
            app:layout_constraintStart_toStartOf="@id/viewPatientAvatar"
            app:layout_constraintEnd_toEndOf="@id/viewPatientAvatar" />

        <!-- Patient Number -->
        <TextView
            android:id="@+id/textPatientNumber"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginTop="4dp"
            android:text="PAT001"
            android:textSize="11sp"
            android:textColor="#757575"
            android:textStyle="bold"
            android:letterSpacing="0.1"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toEndOf="@id/viewPatientAvatar" />

        <!-- Patient Name -->
        <TextView
            android:id="@+id/textPatientName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginTop="4dp"
            android:layout_marginEnd="8dp"
            android:text="Patient Name"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="#212121"
            android:maxLines="2"
            android:ellipsize="end"
            app:layout_constraintTop_toBottomOf="@id/textPatientNumber"
            app:layout_constraintStart_toEndOf="@id/viewPatientAvatar"
            app:layout_constraintEnd_toStartOf="@id/textPatientStatus" />

        <!-- Quick Info Line -->
        <TextView
            android:id="@+id/textPatientGender"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginTop="6dp"
            android:layout_marginEnd="8dp"
            android:text="Gender • Age • DOB"
            android:textSize="13sp"
            android:textColor="#757575"
            android:maxLines="1"
            android:ellipsize="end"
            app:layout_constraintTop_toBottomOf="@id/textPatientName"
            app:layout_constraintStart_toEndOf="@id/viewPatientAvatar"
            app:layout_constraintEnd_toStartOf="@id/textPatientStatus" />

        <!-- Patient Status Badge -->
        <TextView
            android:id="@+id/textPatientStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="32dp"
            android:text="Complete"
            android:textSize="10sp"
            android:textStyle="bold"
            android:background="@drawable/status_badge_background"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            android:textColor="@android:color/white"
            android:maxLines="1"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@id/viewArrow" />

        <!-- Arrow indicator -->
        <TextView
            android:id="@+id/viewArrow"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:text="›"
            android:textSize="24sp"
            android:textColor="#9E9E9E"
            android:gravity="center"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>